<h1>
   <% if(@lists.empty?) %>
    No lists yet
   <% end %>
  <ol>
    <% @lists.each do |item| %>
      <li> <%= link_to item.name, list_path(item) %></li>
    <% end %>
  </ol>
</h1>
<%= link_to "+Add new list",  new_list_path %>
<br>
